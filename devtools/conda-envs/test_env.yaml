name: test

channels:
  - conda-forge

dependencies:
    # Base depends
  - python
  - pip

  # Dependencies for this module
  - seamm
  - psutil
  - fasteners

  # Testing
  - black
  - codecov
  - flake8
  - pytest
  - pytest-cov

  # Documentation
  - pydata-sphinx-theme
  - sphinx-design

  # Pip-only installs
  - pip:
    # Dependencies for this module (not on conda-forge)
    - seamm_slurm
    # Documentation
    - sphinx-copybutton
    - sphinxnotes-strike
